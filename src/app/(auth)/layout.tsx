// Auth route group layout — no sidebar, no nav.
// The Deep Tay background is applied per-page via inline styles.
export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
